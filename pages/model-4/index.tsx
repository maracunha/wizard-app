import Wizard from "../../components/Wizard";

const Model_4 = () => {
  const initialValues = {};
  const onSubmit = () => {};

  return (
    <Wizard initialValues={initialValues} onSubmit={onSubmit}>
      MODEL 4 | Under constrution
    </Wizard>
  );
};

export default Model_4;
