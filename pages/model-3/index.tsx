import Wizard from "../../components/Wizard";

const Model_3 = () => {
  const initialValues = {};
  const onSubmit = () => {};

  return (
    <Wizard initialValues={initialValues} onSubmit={onSubmit}>
      MODEL 3 | Under constrution
    </Wizard>
  );
};

export default Model_3;
