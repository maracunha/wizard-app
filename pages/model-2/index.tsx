import Wizard from "../../components/Wizard";

const Model_2 = () => {
  const initialValues = {};
  const onSubmit = () => {};

  return (
    <Wizard initialValues={initialValues} onSubmit={onSubmit}>
      MODEL 2 | Under constrution
    </Wizard>
  );
};

export default Model_2;
